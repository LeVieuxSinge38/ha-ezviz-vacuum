Photos livrées avec la carte.

`re5-base.png`   — le robot sur sa station, affiché quand il y est
`re5-dessus.png` — vu de dessus, affiché dès qu'il en part

PNG détourés, fond transparent, 512 px de côté suffisent : la carte les
affiche à 96 px au maximum. La carte les prend automatiquement, sans aucune
configuration ; si un fichier manque, elle retombe sur le dessin intégré.
